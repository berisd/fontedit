# Font Edit #

A simple font editor for bitmap and multicolor fonts.

Requires SDL2 library and dev files.

Build e.g. with:
````
cmake -B mybuild
cd mybuild
cmake --build .
````

then run *fontedit*
